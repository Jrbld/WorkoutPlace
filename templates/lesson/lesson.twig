{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ encore_entry_link_tags('lesson') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('lesson') }}
{% endblock %}

{% block title %}Workout Place | Cours{% endblock %}

{% block body %}

<section class="basic-lessons">
    <h1>
        PROFITEZ DE NOS<br>
        DIFFÉRENTS COURS
    </h1>
    <p>(Accessibles à tous)</p>
    <div class="basic" style="background-image: url('{{ asset('build/images/pilates.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'pilates'}) }}">
            PILATES
        </a>
    </div>
    <div class="basic" style="background-image: url('{{ asset('build/images/rpm.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'rpm'}) }}">
            RPM
        </a>
    </div>
    <div class="basic" style="background-image: url('{{ asset('build/images/hiit-step.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'hiit-step'}) }}">
            HIIT STEP
        </a>
    </div>
    <div class="basic" style="background-image: url('{{ asset('build/images/yoga.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'yoga'}) }}">
            YOGA
        </a>
    </div>
</section>

<section class="premium-lessons">
    <h1>LES PREMIUM</h1>
    <p>(Réservés aux abonnés MAX et ULTRA)</p>
    <div class="premium" style="background-image: url('{{ asset('build/images/body-attack.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'body-attack'}) }}">
            BODY ATTACK
        </a>
    </div>
    <div class="premium" style="background-image: url('{{ asset('build/images/cross-training.jpg') }}');">
        <a href="{{ path('lesson.show', {slug: 'cross-training'}) }}">
            CROSS TRAINING
        </a>
    </div>
</section>

{% endblock %}
